// Mileage tracking is intentionally unavailable while the PWA only supports
// reliable foreground location tracking. Keep the implementation in place so
// it can be restored after the tracking lifecycle is made resilient.
export const MILEAGE_TRACKING_ENABLED = false;
